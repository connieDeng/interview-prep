def timSort(nums):
    """
    Implementation of TimSort, python's algo choice for their sort()
    Complexity time: O(n Log n)
    It is also used in Java's Arrays.sort() & python's sort() & sorted()
    sorts small pieces using insertion sort, thenmerges the pieces using merge of merge sort
    """